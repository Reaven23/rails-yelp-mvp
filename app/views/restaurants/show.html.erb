<div>
  <h2><%= @restaurant.name %></h2>
  <h3><%= @restaurant.category %></h3>
  <p><%= @restaurant.address %></p>
  <p><%= @restaurant.phone_number %></p>
  <p><%= @restaurant.category %></p>
  <p><strong>Reviews</strong></p>
  <ul class="list-group">
    <% @restaurant.reviews.each do |review| %>
      <li class="list-group-item">
        <p>Comment : <%= review.content %></p>
        <p>Rating : <%= review.rating %></p>
      </li>
    <% end %>
  </ul>
</div>

<h1>Review this restaurant</h1>


<div class="container">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit 'Send your feedback', class: 'btn btn-primary' %>
  <% end %>
</div>

<%# <p><%= link_to "Add a review for this restaurant",  new_restaurant_review_path(@restaurant), class: "btn btn-primary"  %>
<p><%= link_to "Back to the list of restaurants", restaurants_path, class: "btn btn-primary" %></p>
